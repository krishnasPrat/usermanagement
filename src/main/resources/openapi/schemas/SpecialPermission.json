{
  "title": "SpecialPermission",
  "type": "object",
  "properties": {
    "permissionId": {
      "type": "integer",
      "format": "int64"
    },
    "access": {
      "type": "string",
      "enum": ["ALLOWED", "DENIED"]
    }
  },
  "required": ["permissionId", "access"]
}
